# Fully pinned build environment for CPython 3.12 x64 on Windows.
# Keep transitive dependencies explicit so a clean build does not drift.
altgraph==0.17.5
lxml==6.1.0
packaging==26.2
pefile==2024.8.26
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
pywin32-ctypes==0.2.3
setuptools==83.0.0
